[package]
name = "adapter-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform-independent pieces of the camera adapter: the interactive setup shell and the HTTP snapshot protocol"

[dependencies]
